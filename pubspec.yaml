name: acebot
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0" # Make sure your Dart SDK version is up-to-date

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  provider: ^6.0.0
  speech_to_text: ^6.1.3
  flutter_tts: ^3.2.0
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true